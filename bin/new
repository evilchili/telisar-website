#!/bin/bash -e

SCRIPTDIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
CONTENTDIR=$SCRIPTDIR/../content/posts
TYPE=$1
FILENAME=$(echo ${@:2} | awk '{ print tolower($0) }' | tr -s ' ' '_').md

/usr/local/bin/hugo new -k $TYPE --editor $EDITOR $CONTENTDIR/$FILENAME
