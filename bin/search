#!/usr/bin/env python

from telisar import search
from fire import Fire
import os
import dotenv

SOURCE_PATH_VARIABLE = 'WEBSITE_SOURCE_PATH'
DATA_PATH_VARIABLE = 'SEARCH_INDEX_PATH'

dotenv.load_dotenv()

Fire(search.Searcher(os.environ[SOURCE_PATH_VARIABLE], os.environ[DATA_PATH_VARIABLE]))
